# Scripts

- `setup.sh` — one-time local bootstrap: copies `.env` files, creates the backend virtualenv, installs backend + frontend dependencies.

More helpers (DB seed/reset, migration shortcuts) are added as later phases introduce real data.
