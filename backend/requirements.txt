# --- Core framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0

# --- Settings & validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database ---
sqlalchemy[asyncio]==2.0.36  # [asyncio] pulls in greenlet, required by create_async_engine
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10

# --- Caching ---
redis==5.2.1

# --- Auth / Security ---
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1  # passlib 1.7.4's bcrypt backend probe breaks on bcrypt>=4.1 (see passlib#180)
authlib==1.4.0
itsdangerous==2.2.0

# --- HTTP client (GitHub API, OAuth) ---
httpx==0.28.1

# --- Rate limiting ---
slowapi==0.1.9

# --- Logging ---
structlog==24.4.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0

# --- Lint / type-check ---
ruff==0.8.4
mypy==1.14.0
