import { PlaceholderSection } from "@/components/ui/PlaceholderSection";

export function Blog() {
  return (
    <PlaceholderSection
      title="Blog"
      description="Markdown-powered technical writing with search, tags, filters, and pagination."
    />
  );
}
