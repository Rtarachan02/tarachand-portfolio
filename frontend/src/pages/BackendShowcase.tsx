import { PlaceholderSection } from "@/components/ui/PlaceholderSection";

export function BackendShowcase() {
  return (
    <PlaceholderSection
      title="Backend Engineering"
      description="FastAPI architecture, PostgreSQL schema, REST APIs, authentication, Docker deployment, and system design."
    />
  );
}
