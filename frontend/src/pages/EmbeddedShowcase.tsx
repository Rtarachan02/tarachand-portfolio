import { PlaceholderSection } from "@/components/ui/PlaceholderSection";

export function EmbeddedShowcase() {
  return (
    <PlaceholderSection
      title="Embedded Systems"
      description="High-speed FPGA data acquisition, dual-core synchronization, ARM architecture, DMA, circular buffers, and Linux device drivers — with interactive diagrams."
    />
  );
}
